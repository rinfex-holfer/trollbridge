export const enum CharStateKey {
    IDLE = 'IDLE',
    GO_ACROSS = 'GO_ACROSS',
    SURRENDER = 'SURRENDER',
    DEAD = 'DEAD',
    BONES = 'BONES',
}

export const enum CharAnimation {
    WALK = 'walk',
    IDLE = 'idle',
    SURRENDER = 'scared',
    DEAD = 'dead',
}